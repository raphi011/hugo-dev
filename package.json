{
  "name": "hugo-dev",
  "private": true,
  "scripts": {
    "build:highlight": "rehype exampleSite/public -o",
    "build": "HUGO_PARAMS_COMMITHASH=$(git rev-parse HEAD) hugo --source exampleSite && bun run build:highlight",
    "dev": "hugo server --source exampleSite"
  },
  "devDependencies": {
    "@shikijs/rehype": "^3.4.2",
    "@tailwindcss/cli": "^4.1.18",
    "@tailwindcss/typography": "^0.5.19",
    "rehype-cli": "^12.0.1"
  }
}
